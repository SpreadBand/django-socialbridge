from django.conf.urls.defaults import patterns, url, include

import views

urlpatterns = patterns('',
  # url(r"^post_message/(?P<service>\w+)/$", views.post_message, name="socialbridge-post-message"),
)


